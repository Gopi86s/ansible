- name: play level variables
  hosts: localhost
  connection: localhost
  vars_files:
  - vars.yaml
  tasks: 
  - name: print the course info
    vars: 
    ansible.builtin.debug:
     msg: "Course name is: {{COURSE}}, Trainer: {{TRAINER}}, Duriation: {{DURIATION}}"